meta {
  name: Actualizar Funko - Precio Invalido
  type: http
  seq: 13
}

put {
  url: {{host}}/funkos/{{funkoId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

body:json {
  {
    "nombre": "Funko Actualizado",
    "precio": 0,
    "categoriaId": "{{categoriaId}}"
  }
}

assert {
  res.status: eq 400
}
