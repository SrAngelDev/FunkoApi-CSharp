meta {
  name: Obtener por ID
  type: http
  seq: 3
}

get {
  url: {{host}}/funkos/{{funkoId}}
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.id: isDefined
  res.body.nombre: isDefined
}
